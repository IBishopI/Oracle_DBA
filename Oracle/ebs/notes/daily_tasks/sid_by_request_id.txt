#################################################################
SID serial by request ID
#################################################################
SELECT ses.sid,  
ses.serial#  
FROM v$session ses,  
v$process pro  
WHERE ses.paddr = pro.addr  
AND pro.spid IN (SELECT oracle_process_id  
FROM apps.fnd_concurrent_requests  
WHERE request_id =&request);